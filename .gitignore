.venv/
*.retry
.vault-pass

# terraform (.terraform.lock.hcl is NOT ignored - it's the provider version
# lock, should be committed like uv.lock)
.terraform/
terraform.tfstate
terraform.tfstate.*
terraform/.env

# generated by terraform/inventory.tf - derived from tfstate, not source of truth
ansible/inventory/vms.yml

kubernetes/.env
kubernetes/kubeconfig

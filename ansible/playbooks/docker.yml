---
- name: Install Docker
  hosts: docker
  become: true
  gather_facts: true
  roles:
    - docker

- name: Deploy Komodo (self-hosted GitOps for docker compose)
  hosts: komodo
  become: true
  gather_facts: true
  roles:
    - komodo
